const assert=require('node:assert/strict');const test=require('node:test');const http=require('node:http');
process.env.BOS_ENCRYPTION_KEY='0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef';
const {requestHandler}=require('../server');
function request(method,url,body={},headers={}){return new Promise((resolve,reject)=>{const server=http.createServer(requestHandler).listen(0,()=>{const port=server.address().port;const payload=JSON.stringify(body);const req=http.request(`http://127.0.0.1:${port}${url}`,{method,headers:{'content-type':'application/json','content-length':Buffer.byteLength(payload),...headers}},res=>{let text='';res.setEncoding('utf8');res.on('data',c=>text+=c);res.on('end',()=>{server.close();resolve({statusCode:res.statusCode,body:text})})});req.on('error',e=>{server.close();reject(e)});req.end(payload)})})}
test('WhatsApp connection endpoint validates tenant credentials',async()=>{const response=await request('POST','/api/v1/integrations/whatsapp/connect',{phoneNumberId:'123456',accessToken:'token'},{'x-tenant-id':'tenant-test'});assert.ok([201,422].includes(response.statusCode));if(response.statusCode===201){const body=JSON.parse(response.body);assert.equal(body.provider,'whatsapp_cloud');assert.equal(body.status,'connected')}});
test('WhatsApp status endpoint exposes connection state without secrets',async()=>{const response=await request('GET','/api/v1/integrations/whatsapp',{}, {'x-tenant-id':'tenant-test'});assert.ok([200,422].includes(response.statusCode));if(response.statusCode===200){const body=JSON.parse(response.body);assert.ok(['connected','not_connected'].includes(body.status));assert.equal(Object.prototype.hasOwnProperty.call(body,'accessToken'),false)}});
