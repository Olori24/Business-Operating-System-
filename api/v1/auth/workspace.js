const { getSession, parseCookies } = require('../../../packages/auth/email_auth');
const { getProductionStore } = require('../../../packages/persistence/production_store');
module.exports=async function(req,res){if(req.method!=='GET')return res.status(405).json({error:{code:'METHOD_NOT_ALLOWED'}});try{const session=await getSession(parseCookies(req).bos_session);if(!session)return res.status(401).json({error:{code:'AUTH_REQUIRED'}});const store=await getProductionStore();if(!store)return res.status(503).json({error:{code:'DATABASE_NOT_CONFIGURED'}});const r=await store.pool.query("SELECT value FROM bos_records WHERE record_type='workspace' AND value->>'userId'=$1 ORDER BY created_at DESC LIMIT 1",[session.user_id]);return res.status(200).json({workspace:r.rows[0]?.value||null,user:{id:session.user_id,email:session.email,name:session.name}})}catch(e){return res.status(500).json({error:{code:'WORKSPACE_LOOKUP_FAILED',message:e.message}})}};
